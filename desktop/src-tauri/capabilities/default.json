{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for InvestBrain main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
